
REPLACE:alliance_can_exist = {
	$first$ = {
		OR = {
			opinion = { target = $second$ value >= 0 }
			is_ai = no
		}
		not = { has_blocked_treaties = $second$ }
		not = { is_subject_of = $second$ }
		not = { is_rival_of = $second$ }
		modifier:allowed_alliance = yes
		NOT = {
			is_member_of_international_organization_of_type = {
				type = coalition
				target = $second$
			}
		}
		NOT = {
			in_union_with = $second$
		}
		is_rebel_country = no
	}
	$second$ = {
		OR = { 
			opinion = { target = $first$ value >= 0 }
			is_ai = no
		}
		not = { has_blocked_treaties = $first$ }
		not = { is_subject_of = $first$ }
		not = { is_rival_of = $first$ }
		modifier:allowed_alliance = yes
		NOT = {
			is_member_of_international_organization_of_type = {
				type = coalition
				target = $first$
			}
		}
		is_rebel_country = no
	}
	can_create_subject_alliance = {
		actor = $first$
		recipient = $second$
	}
	NOT = {
		 AND = {
			$first$ = {
				is_hegemon = yes
			}
			$second$ = {
				is_hegemon = yes
			}
		}
	}
}

REPLACE:can_upgrade_country_rank_empire = {
	is_great_power = yes
	trigger_if = {
		limit = { 
			country_exists = c:PAP
			NOT = { tag = PAP }
			religion = religion:catholic 
		}
		c:PAP = {
			opinion = { target = root value > 100 }
		}
	}
	OR = {
		total_population > 10000
		total_true_faith_population > 7500
		total_accepted_culture_population > 5000
	}
	prestige > 35
	is_subject = no
}
REPLACE:can_upgrade_country_rank_kingdom = {
	OR = {
		total_population > 2000
		total_true_faith_population > 1500
		total_accepted_culture_population > 1000
		AND = {
			has_game_rule = rank_restriction_by_numbers
			is_member_of_international_organization = international_organization:hre
			OR = {
				total_population > 2000
				total_true_faith_population > 1500
				total_accepted_culture_population > 1000
			}
			international_organization:hre = {
				any_international_organization_member = {
					country_rank_level = 3
					count < 6
				}
			}
		}
	}
	prestige > 25		
	is_subject = no
}
REPLACE:can_upgrade_country_rank_duchy = {
	OR = {
		total_population > 200
		total_true_faith_population > 150
		total_accepted_culture_population > 100
	}
	prestige > 12.5
}

REPLACE:can_country_have_tusi = {			#UU temp until bugfix?
	exists = international_organization:middle_kingdom
	OR = {
		is_leader_of_international_organization = international_organization:middle_kingdom
		AND = {
			is_subject = yes
			is_subject_type = tusi			#UU this line previously bugged
		}
		AND = {
			exists = international_organization:middle_kingdom.leader_country
			is_subject_of = international_organization:middle_kingdom.leader_country
			#culture = overlord.culture		#UU this seems to cause some errors.
		}
	}
}